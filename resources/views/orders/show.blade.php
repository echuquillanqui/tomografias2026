@extends('layouts.app')
@section('content')
<div class="container"><section class="clinic-page-hero mb-4"><div class="d-flex justify-content-between"><div><div class="clinic-eyebrow mb-2">Detalle de orden</div><h1 class="display-6 fw-bold">{{ $order->codigo_orden ?? '—' }}</h1><p class="mb-0 opacity-75">{{ $order->patient->nombres }} {{ $order->patient->apellidos }} · {{ $order->fecha_orden->format('d/m/Y') }}</p></div><div><a class="btn btn-light" href="{{ route('orders.edit',$order) }}">Editar</a><a class="btn btn-outline-light" href="{{ route('orders.index') }}">Volver</a></div></div></section><div class="row g-4"><div class="col-lg-4"><div class="card clinic-card p-4 h-100"><h5 class="fw-bold">Datos generales</h5><p><strong>Paciente:</strong><br>{{ $order->patient->dni }} - {{ $order->patient->nombres }} {{ $order->patient->apellidos }}</p><p><strong>Convenio:</strong><br>{{ $order->agreement->nombre_institucion }}</p><p><strong>Médico solicitante:</strong><br>{{ $order->medicoSolicitante?->nombre_completo ?? '—' }}</p><p><strong>Tipo de pago:</strong><br>{{ $order->tipo_pago ?? '—' }}</p><p><strong>Médico informe:</strong><br>{{ $order->medicoInforme?->nombre_completo ?? '—' }}</p><span class="badge badge-role align-self-start">{{ $order->estado }}</span></div></div><div class="col-lg-8"><div class="card clinic-card"><div class="card-body p-0"><table class="table table-clinic mb-0"><thead><tr><th>Examen</th><th>Contraste</th><th>Estado</th><th>Precio</th><th>Comisión</th></tr></thead><tbody>@foreach($order->orderExams as $item)<tr><td class="fw-bold">{{ $item->exam->nombre_examen }}</td><td>{{ $item->tipo_contraste }}</td><td>{{ $item->estado }}</td><td>S/ {{ $item->precio }}</td><td>{{ $item->comision_monto ? 'S/ '.$item->comision_monto : '—' }}</td></tr>@endforeach</tbody><tfoot><tr><th colspan="3" class="text-end">Subtotal</th><th>S/ {{ $order->subtotal }}</th><th></th></tr><tr><th colspan="3" class="text-end">Descuento</th><th>S/ {{ $order->descuento }}</th><th></th></tr><tr><th colspan="3" class="text-end">Total</th><th>S/ {{ $order->total }}</th><th></th></tr></tfoot></table></div></div>@if($order->observaciones)<div class="card clinic-card p-4 mt-4"><h6 class="fw-bold">Observaciones</h6>{{ $order->observaciones }}</div>@endif</div></div></div>
@endsection
